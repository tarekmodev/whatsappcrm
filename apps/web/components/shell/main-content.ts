/**
 * The id of the `<main>` landmark. Shared by the skip link, the focus handling on
 * route change, and the layout that renders it, so the three cannot drift.
 */
export const MAIN_CONTENT_ID = 'main-content';
